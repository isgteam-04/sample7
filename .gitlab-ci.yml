$ docker-compose upimage:  docker

services:
    - docker:dind
    
stages:
    - test
    - deploy
    
test:
    stage: test
    only:
        - master
    script:
        - echo run test in this section
        
step-deploy-prod:
    stage: deploy
    only:
        - master
    script:
        - sudo docker-compose up -d --build
        
    environment: master
    when: manual